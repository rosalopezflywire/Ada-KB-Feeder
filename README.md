# Ada-KB-Feeder
A lightweight browser-based tool that converts Flywire's GitLab HTML documentation into clean Markdown and packages it as JSON ready to upload to Ada's Knowledge Base via the API. Supports bulk conversion, article preview and editing, duplicate detection, and Postman-ready export.
